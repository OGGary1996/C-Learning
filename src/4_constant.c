#include <stdio.h>

int main() {
    // Constants are fixed values that do not change during the program execution.
    // Like final in Java, const in C is used to define a constant.
    const int PI = 3.14159;
    printf("The value of PI is %d.\n", PI);
    return 0;
}